node_modules
dist
.env
.env.bak*
.env.railway
.env.local
_railway_sync.sh
*.log
# DB 덤프·진단 SQL (CLAUDE.md §1 — repo 커밋 금지). 마이그레이션은 예외.
*.sql
!drizzle/migrations/*.sql
.DS_Store

# Archives & snapshots
*.tar.gz

# Legacy dev scripts (Railway only, no local dev)
dev-start.bat
dev-start.ps1
dev.ps1
setup.bat
cleanup.bat

# Preview images (outdated)
preview-*.png

# Legacy migration scripts
run-migration.mjs
run-migration-v2.mjs

# Legacy docs
porting-strategy.md

# Claude Code
.claude/credentials.json
.claude/scheduled_tasks.lock
.commitmsg

# System junk
NVIDIA Corporation/
package-lock.json
backups/
todo.md
analyze_schema.py
.claude/settings.local.json
.claude/worktrees/

# OCR 실험 산출물 (실제 전표 데이터 포함)
test-ocr-results/

# 로컬 전용 문서 보관소 (실매출·실명 포함 — public repo라 커밋 금지)
docs/_local/
