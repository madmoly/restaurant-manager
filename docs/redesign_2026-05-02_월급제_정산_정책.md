# 월급제 인건비 정산 정책 재설계 사양서

> 작성: 2026-05-02 (Cowork)
> 상태: 사양 확정 대기 (사용자 검토 후 Code 핸드오프)
> 트리거: LaborCostPage에서 월급제 직원 합계가 계약월급과 다른 값(예: 270만 → 225만/176만)으로 표시. 시간 부족 시 자동 차감, 표시값 ≠ 실제 지급액 괴리 발생.
> 관계: 기존 `redesign_2026-05-02_계약_직원_인건비.md` §2.4 "인건비 산정 분기 표"는 본 사양으로 **supersede됨**. 5인 여부에 따른 분모 분기·시간 비례 합계 산식 폐기.

---

## 0. 핵심 판단

월급제는 본질적으로 **고정급 + 명시적 차감만 허용**하는 임금 형태다. 현재 시스템은 매 시프트마다 시급 환산 후 누적해 합계로 사용한다 → 시급제와 동일한 운영 → 월급제의 정의 위배.

5인 미만/이상은 **연차 발생 의무 유무**에만 영향한다. 분모(소정근로시간)·휴무일 수·시급 가이드·합계 산식은 5인 여부와 무관하다.

해결 방향: **합계 = 계약월급 - 미근무시간 차감(시간 비례) - 입사·퇴사 일할 보정**, **분모는 209h로 통일**, **5인 여부는 연차 발생 추적에만 적용**.

---

## 1. 현재 결함 (재진단)

### 1.1 월급제 합계 = 시간 비례 누적
`server/routers/schedules.ts:1080-1117`:
```
let stdHours = computeMonthlyStandardHours(null, true); // 209h fallback
...
const wage = computeWageForShift({
  wageType: 'monthly',
  wageAmount: 월급,
  hoursWorked: hours,
  monthlyStandardHours: stdHours,
});
companyMap[company].employees[empKey].totalWage += wage;  // ← Σ 누적
```
시프트마다 `시간 × (월급/분모)`를 누적해 `totalWage` 산출. 결과:
- 김정란(월급 270만, 218h, 5인 미만 분기 적용 분모) → 합계 2,257,644원 (-16.4%)
- 진재이(월급 320만, 144h) → 합계 1,767,452원 (-44.8%)

월급제임에도 시간 부족 시 임금이 자동 차감됨.

### 1.2 분모 5인 분기
`StaffPage.tsx:1775`:
```
const divisor = monthlyContractHours + (form.over5Employees ? 8 : 0);
```
5인 이상에 +8h 가산하는 분기. 사용자 의사: 분모는 5인 여부와 무관하게 209h 통일.

### 1.3 표시값 ≠ 지급액
`emp.effectiveMonthly = emp.totalWage` (schedules.ts:1210) → 합계가 곧 "실효 월급"으로 표시되지만 실제로는 계약월급에서 시간 비례로 깎인 값. 운영자가 "이 금액 그대로 지급"으로 오해할 위험.

### 1.4 일할/결근 정책 부재
- 입사·퇴사 월의 일할 계산 식 없음. 시프트가 적게 잡힌 결과로 합계만 작아짐.
- 결근일을 명시적으로 입력할 UI·필드 없음. 시프트 부재 = 결근 vs 운영 누락 구분 불가.

---

## 2. 새 정산 룰

### 2.1 5인 여부의 영향 범위 (재정의)

| 영역 | 5인 이상 | 5인 미만 |
|---|---|---|
| 분모 (소정근로시간) | **209h** | **209h** |
| 시급 가이드 | 월급 ÷ 209 | 월급 ÷ 209 |
| 일급 가이드 | 시급 × 8 | 시급 × 8 |
| 월급 가이드 | 계약월급 | 계약월급 |
| 합계 산식 | **§2.2 단일 산식** | **§2.2 단일 산식** |
| 연차 자동 발생 | 적용 (입사일 기준) | 미적용 (포괄수당) |
| 주휴 | 209h 분모에 이미 포함 | 209h 분모에 이미 포함 |

→ 5인 분기는 **연차 발생 로직 한 곳에만** 잔존. 그 외 모든 임금 산정·표시 코드에서 5인 분기 제거.

### 2.2 월급제 합계 산식

```
합계 = 계약월급
       - 일할 보정 (입사·퇴사 월에만)
       - 미근무시간 차감
```

상세:

#### (a) 일할 보정 (입사·퇴사 월)
- 입사 월: `월급 × (입사일 ~ 월말 일수) / 월간 일수`
- 퇴사 월: `월급 × (월초 ~ 퇴사일 일수) / 월간 일수`
- 동월 입·퇴사: 두 비율의 곱 적용
- 그 외 월: 일할 미적용 (= 계약월급 그대로)

월간 일수는 **달력일수**(28~31). 소정근로일 기반은 휴무일 정의가 변동성 있어 분쟁 소지.

#### (b) 미근무시간 차감 (시간 기준 — 사용자 결정)
```
미근무시간 = max(0, 소정근로시간_월 - 실근무시간)
미근무차감 = 미근무시간 × (월급 / 209h)
```

- 소정근로시간_월 = 209h (또는 일할 보정 시 209h × 일할비율)
- 실근무시간 = 시프트 합산
- 합계는 0원 미만으로 내려가지 않음 (`max(0, …)`)

> 사용자 결정 (a 옵션): "미근무시간 × (월급/209) 차감" — 결근일을 별도로 입력하지 않고 시프트 부재 시간만큼 자동 차감. 운영자가 시프트를 빠뜨리면 임금이 깎이는 위험은 있으나, 결근 입력 UI 신설 비용 회피 + 시프트 = 사실상 출퇴근 기록 SSOT라는 운영 전제.

#### 검증식
- 시프트 합산이 209h 이상이면 미근무차감 = 0 → 합계 = 계약월급
- 시프트 합산이 209h 미만이면 부족분만큼 시급 비례 차감
- 입사·퇴사 월은 209h를 일할 비율로 축소한 값과 비교

### 2.3 가이드 환산 (LaborCostPage 표시)

월급제 직원:
- 시급 가이드 = 월급 ÷ 209
- 일급 가이드 = 시급 × 8
- 월급 가이드 = 계약월급
- 5인 여부에 따른 분모 차이 표기 **제거** ("5인 미만 — 연차 분모 제외" 라벨 삭제)

실효:
- 실효 시급 = 합계 ÷ 실근무시간
- 실효 일급 = 합계 ÷ 실근무일수
- 실효 월급 = 합계
- 가이드와 다른 경우 차이율(±%) 표시 유지

### 2.4 시급제는 본 사양 범위 외

시급제는 현행 산식(시간 × 시급 + 주휴 별도/포함 분기) 유지. 본 사양은 월급제 합계 결함만 다룸.

---

## 3. 데이터 모델 영향

### 3.1 변경 없음
- `employment_electronic_contracts.over5Employees` 컬럼: 유지 (서명 시점 박제, 연차 추적용)
- `employee_wage_history`: 유지

### 3.2 마이그레이션 불필요
스키마 변경 없음. 코드 로직만 수정.

### 3.3 결근일 입력 UI 신설 안 함 (현행 유지)
사용자 결정 (a 옵션). 시프트 부재 = 미근무 = 자동 차감.

향후 결근/연차/대체휴무 입력 UI를 신설하게 되면, 그 입력값으로 "유급 처리되는 미근무시간"을 차감 대상에서 제외하는 보정이 필요. 본 사양 범위 외.

---

## 4. 코드 변경

### 4.1 `server/routers/schedules.ts:laborCostByCompany`

#### 변경 전 (요약)
```ts
// 시프트마다 시간 비례 누적
const wage = computeWageForShift({ wageType, wageAmount, hoursWorked: hours, monthlyStandardHours: stdHours });
emp.totalWage += wage;
```

#### 변경 후
시급제·임시근로자는 현행 누적 유지. 월급제만 분기:

```ts
if (shiftWageType === 'monthly') {
  // 시프트 시간만 누적, 임금 누적 안 함
  emp.totalHours += hours;
} else {
  // 시급제/일급제: 현행
  emp.totalWage += computeWageForShift(...);
  emp.totalHours += hours;
}
```

월말 일괄 단계에서 월급제 직원 합계 산출:
```ts
for (const emp of monthlyEmployees) {
  const monthlyWage = Number(emp.wageAmount);
  const proration = computeProration(emp.hireDate, emp.resignDate, year, month);
  // proration: { proratedMonthlyWage, proratedStdHours }
  const expectedHours = 209 * proration.ratio;
  const shortfall = Math.max(0, expectedHours - emp.totalHours);
  const shortfallDeduction = shortfall * (monthlyWage / 209);
  emp.totalWage = Math.max(0, proration.proratedMonthlyWage - shortfallDeduction);
}
```

### 4.2 `helpers/wage.ts`

#### 신규: `computeProration`
```ts
export function computeProration(
  hireDate: string | null,
  resignDate: string | null,
  year: number,
  month: number
): { ratio: number; proratedMonthlyWage: number; proratedStdHours: number };
```
- 입사·퇴사 월이 아니면 `ratio = 1`
- 입사월: `(월말 - 입사일 + 1) / 월간일수`
- 퇴사월: `(퇴사일 - 월초 + 1) / 월간일수`
- 동월 입·퇴사: 둘의 결합

#### 변경: `computeMonthlyStandardHours`
- `over5Employees` 파라미터 무시 또는 deprecate. 항상 209h 반환.
- 호출부 정리 후 파라미터 자체 제거.

#### 변경: `computeGuideWage`
- 분기 폐기. 월급제는 단순 `월급/209`로 시급 환산.

### 4.3 `client/src/pages/StaffPage.tsx`

`L1775` 분모 분기 폐기:
```ts
// 변경 전
const divisor = monthlyContractHours + (form.over5Employees ? 8 : 0);
// 변경 후
const divisor = 209;
```

`L2043` 표시 라벨 삭제:
```tsx
// 삭제
월소정근로 {monthlyContractHours}h{form.over5Employees ? " + 연차 8h" : " (5인 미만 — 연차 분모 제외)"}
// 변경 후
월소정근로 209h
```

### 4.4 `client/src/pages/LaborCostPage.tsx`

`L484-488` "5인 미만 — 연차 분모 제외" 라벨 삭제:
```tsx
// 삭제
{!emp.over5Employees && (
  <span className="ml-1 text-[10px] text-amber-600 dark:text-amber-400">5인 미만 — 연차 분모 제외</span>
)}
```

가이드/실효 표시는 유지하되, 5인 분기 제거.

### 4.5 연차 발생 로직

`leaveBalance.ts` / `helpers/labor.ts:check5PlusEmployee`는 그대로 유지. 5인 이상 직원에게만 입사일 기준 연차 자동 발생 → leave_transactions 적립. 본 사양 범위 외.

---

## 5. 작업 순서 (Code 핸드오프)

### Phase 1 — 합계 산식 변경
1. `helpers/wage.ts`: `computeProration` 신규 함수 추가, 단위 테스트
2. `schedules.ts:laborCostByCompany`:
   - 월급제 시프트 임금 누적 라인 제거
   - 월말 일괄 단계 추가 (proration + shortfall deduction)
   - `effectiveMonthly = totalWage` 그대로 유지 (값의 의미만 바뀜)
3. `schedules.ts:workSummaryByEmployee` 동일 변경

완료 조건: 빌드 통과 + 천호점 5월 정산 결과에서 월급제 직원 합계 = 계약월급 (209h 이상 근무 시) 또는 계약월급 - 미근무차감 (209h 미만 시) 확인.

### Phase 2 — 분모 통일
4. `helpers/wage.ts:computeMonthlyStandardHours`: 209h 고정 반환
5. `helpers/wage.ts:computeGuideWage`: 5인 분기 제거
6. `StaffPage.tsx:1775`: divisor = 209 고정
7. `StaffPage.tsx:2043`: 라벨 단순화

완료 조건: 신규 계약서 작성 시 5인 여부와 무관하게 시급 가이드 = 월급/209 일치.

### Phase 3 — UI 정리
8. `LaborCostPage.tsx:486`: "5인 미만 — 연차 분모 제외" 라벨 삭제
9. 카드 표시 검수: 가이드·실효·합계 라인 의미가 새 산식에 맞는지 확인

완료 조건: 5월 정산 화면에서 월급제 직원 합계 = 계약월급(또는 일할/차감 적용분) 표시.

### Phase 4 — 회귀 테스트
10. 시나리오 1~6 (§6) 검증
11. 기존 시급제 직원 정산이 영향받지 않았는지 확인 (회귀)

---

## 6. 테스트 케이스

| # | 조건 | 기대 결과 |
|---|---|---|
| 1 | 월급제 직원, 월 209h 이상 근무 | 합계 = 계약월급 |
| 2 | 월급제 직원, 월 200h 근무 (9h 부족) | 합계 = 월급 - 9 × (월급/209) |
| 3 | 월급제 직원, 월 144h 근무 (65h 부족) | 합계 = 월급 - 65 × (월급/209), 0원 미만 X |
| 4 | 월급제 직원, 5/2 입사, 5월 209h × (30/31) ≈ 202h 근무 | 합계 = 월급 × (30/31) (일할만, 차감 0) |
| 5 | 월급제 직원, 5/2 입사, 5월 100h만 근무 | 합계 = 월급 × (30/31) - (202 - 100) × (월급/209) |
| 6 | 월급제 직원, 5인 이상 vs 5인 미만 동일 월급·시간 | 합계·시급 가이드 모두 동일 |
| 7 | 시급제 직원 | 현행 그대로 (회귀) |
| 8 | StaffPage 계약서 모달, 5인 여부 토글 | 시급/일급 가이드 변동 없음 |
| 9 | LaborCostPage 카드, 5인 미만 월급제 | "연차 분모 제외" 라벨 비표시 |
| 10 | 5인 이상 직원 연차 발생 | 입사일 기준 자동 적립 (회귀) |

---

## 7. 리스크

### 7.1 운영자 시프트 누락 → 임금 자동 차감
- 시프트를 빠뜨리면 미근무시간으로 처리되어 임금 차감.
- 완화: 정산 화면에 "근무시간 209h 미만 — 시프트 누락 가능성 확인 필요" 경고 배너 추가 (Phase 5 후속).
- 완화: 결근일 명시적 입력 UI 신설 시 차감 사유를 분리 (별도 핸드오프).

### 7.2 일할 계산 기준 분쟁 가능성
- 달력일수 vs 소정근로일수 간 임금 차이 발생.
- 본 사양은 달력일수 채택. 취업규칙·근로계약서 본문에 동일 기준 명시 필요(별도 핸드오프).

### 7.3 기존 정산 결과와 차이
- 5월 정산이 이미 진행 중이면 합계가 변동됨.
- 운영 영향: 점장이 "왜 이번 달부터 금액이 다르냐"고 질의 가능.
- 완화: Phase 1 배포 직후 운영 안내 1회 (master 알림 또는 시스템 공지).

---

## 8. 미해결 / 후속 핸드오프 후보

- 결근일·무급휴가 명시 입력 UI (현행 시프트 부재 = 미근무 자동 처리 한계)
- 시프트 누락 경고 배너
- 일할 계산 기준 (달력일수 vs 소정근로일수) 취업규칙 정합성 검토
- 시급제 직원의 일할 보정 (현재 시급제는 시간만큼만 지급이라 일할 보정 자체가 무의미하지만, 입사·퇴사 시 주휴수당 발생 기준일은 별도 검토 필요)

---

## 부록 A — 기존 redesign 문서와의 관계

`docs/redesign_2026-05-02_계약_직원_인건비.md` 의 다음 섹션은 본 사양으로 supersede됨:

| 기존 위치 | 기존 내용 | 본 사양에서 |
|---|---|---|
| §2.4 인건비 산정 분기 표 | 5인 이상/미만에 따라 분모 분기, 합계 = 시간 비례 | §2.1 + §2.2 단일 산식, 분모 209h 통일 |
| §3.1 affiliated_companies | 5인 여부를 소속회사에 박제 | 유지 (본 사양과 무관) |
| §5.3 schedules.ts 임금 분기 | 4종 분기 | 시급제 2종(주휴포함/별도)만 잔존, 월급제는 단일 산식 |
| §5.4 helpers/wage.ts | 5인 분기 함수 | 분기 제거, 209h 고정 |

기존 §3.1·§3.2 (affiliated_companies, taxMode 등)은 본 사양과 독립이며 그대로 유지된다.

---

## 부록 B — 진행 로그

(Code 세션 진행 시 한 줄씩 append)

```
YYYY-MM-DD Code(SHA) — Phase X 단계 N 완료 / 미해결: ...
```
