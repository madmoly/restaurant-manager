PDF 내보내기 autoTable 호출 방식 수정

- doc.autoTable(opts) 프로토타입 메서드 → autoTable(doc, opts) 함수형 호출로 복원
- Vite ESM 빌드에서 side-effect import로는 프로토타입 등록이 안됨
- atModule.default || atModule.autoTable fallback으로 ESM/CJS 양쪽 호환
- 빌드된 chunk에서 default/autoTable 모두 named export 확인됨
