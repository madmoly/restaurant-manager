직원 화면 전용 글씨 확대(돋보기) 기능

- 직원(staff) 역할만 헤더에 돋보기 아이콘 표시
- 클릭할 때마다 기본 → +30% → +60% → +90% → 기본 순환
- 메인 콘텐츠 영역 font-size를 퍼센트로 적용 (rem/em 기반 요소 전체 확대)
- 선택값 localStorage에 저장 (새로고침 시 유지)
- 유저 드롭다운 메뉴에서도 현재 설정 확인/변경 가능
- 활성화 시 돋보기 아이콘 하이라이트 + 배율 뱃지 표시
