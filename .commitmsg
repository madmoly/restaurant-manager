fix: require user confirmation before date change and counterparty update

- Invoice date: shows toast with "변경" button instead of auto-applying
- Counterparty info: shows toast with "반영" button, calls dedicated API
- Added POST /api/ocr/update-counterparty-info endpoint (user-initiated)
- Removed auto-update of counterparty info from OCR flow

Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>
