feat: OCR auto-date and counterparty info sync

1. Transaction date extraction: OCR reads invoice date and auto-sets
   purchase date if different from current (with toast notification)
2. Counterparty info sync: OCR extracts contact name/phone from invoice,
   auto-updates counterparty DB if changed
3. Added transactionDate and counterpartyInfo to OCR prompt and response

Co-Authored-By: Claude Opus 4.6 <noreply@anthropic.com>
