/**
 * OCR 프롬프트 모듈
 *
 * - promptV2: Upstage Document Parse 텍스트 입력 → Claude 구조화/해석/검증
 * - promptV1ImageFallback: Claude Vision 이미지 직접 분석 (기존 경로 / fallback)
 */

// ────────────────────────────────────────────────────────────────────────────
// v2: Upstage 텍스트 해석 프롬프트
// ────────────────────────────────────────────────────────────────────────────

export function promptV2(upstageContext: string, profileHint: string, correctionNote?: string): string {
  return `당신은 한국 식당/매장의 매입 전표 데이터를 정규화하는 역할입니다.

OCR은 이미 Upstage Document Parse가 수행했습니다. 당신은 **이미지를 보지 않고** 아래 텍스트/HTML만으로 작업합니다.

## 입력 데이터

아래는 Upstage Document Parse가 뽑은 문서 내용입니다:

\`\`\`
${upstageContext}
\`\`\`

## 당신의 임무

1. **거래 품목 테이블만** 추출 (공급자/공급받는자 정보 테이블은 거래처 정보로만 활용)
2. 셀 병합/분리 오류를 **휴리스틱으로 보정**
3. 낙서·메모·체크표시를 **품목에서 제외**
4. 각 행을 스키마에 맞춰 정규화

## 작업 순서

### STEP 1: 문서 유형 판정
- Upstage가 분리한 표 중 "품목 테이블"을 찾는다 (헤더: 일자/품목/규격/수량/단가/공급가액 등)
- 공급자/공급받는자 정보 테이블과 혼동 금지
- 행·열 판정은 markdown이 아니라 표 HTML의 <td> 경계를 기준으로 하세요.

### STEP 2: 거래처명 + 날짜 추출
- 공급자 상호명 = counterpartyName (공급받는자가 아님)
- 거래일 = transactionDate (YYYY-MM-DD)

### STEP 2.5: 열 구조 확정 + 관계식 판별

거래명세표의 열 구성은 발행 업체마다 다릅니다. **양식을 가정하지 말고 헤더를 먼저 읽으세요.**
흔한 구성 예: 품목/규격 | 단위 | 수량 | 단가 | 공급가액 | 부가세 | 비고
(월일 컬럼이 있는 양식도, 없는 양식도 있습니다. 합계금액 컬럼도 있을 수도 없을 수도 있습니다.)

**단가 기준을 먼저 판별하세요.** 부가세 값이 있는 과세 행 1~2개를 골라 확인:
  ⓐ 수량 × 단가 = 공급가액 + 부가세   → 단가가 **부가세 포함** 단가
  ⓑ 수량 × 단가 = 공급가액            → 단가가 **부가세 별도** 단가

판별한 유형을 **문서 전체에 일관 적용**하세요. 행마다 다르게 해석하면 안 됩니다.
면세 행은 두 유형 모두 "공급가액 = 수량×단가, 부가세 칸 공란"이므로 판별 근거가 되지 못합니다.
과세 행이 하나도 없으면 판별하지 말고 note에 "단가 기준 판별 불가"라고 적으세요.

**각 행을 추출한 직후 판별한 식으로 자기검증하세요.**
맞지 않으면 값을 지어내지 말고 **열 매핑을 다시 확인**하세요.

⚠ **관계식은 검증·열매핑 확인용입니다. 값을 재계산해 출력하는 용도가 아닙니다.**
   quantity/unitPrice/supplyAmount/vatAmount/lineTotal에는 **문서에 적힌 숫자를 그대로** 옮기세요.
   자기검증이 계속 실패하면 문서 값을 유지한 채 uncertain=true로 표시하세요.
   (예외: 합계금액 컬럼이 없어 lineTotal을 공급가액+부가세로 합성하는 경우만 계산 허용)

자주 나오는 오류:
  - 면세 행은 부가세·비고 칸이 비어 있어 열이 왼쪽으로 밀리기 쉽습니다.
    공급가액을 부가세 칸에서 읽지 않았는지 확인하세요.
  - 수량이 "1.000", "30.000"처럼 소수점 3자리로 표기되는 양식이 있습니다.
    "30.000"은 30이지 30000이 아닙니다.
  - ⓐ 유형에서는 단가가 공급가액보다 큽니다. ⓑ 유형에서는 같거나 작습니다.
    판별한 유형과 어긋나면 열을 잘못 읽은 것입니다.

### STEP 3: 품목 행 추출 + 셀 오류 보정

다음 휴리스틱을 순서대로 적용:

**[H1] 빈 셀 + 다음 행 단독 숫자 = 병합**
  Upstage는 가끔 단가/금액을 다음 행으로 분리합니다.
  예:
    | 간마늘 | 1 | (빈 셀) |
    | (빈) | (빈) | 10,000 |
  → 이전 행의 빈 단가 셀에 10,000을 채움 + 다음 행 삭제

**[H6] 품목명 줄바꿈 분리 복원 (중요)**
  긴 품목명이 셀 폭을 넘어 두 행으로 쪼개지는 경우가 잦습니다.
  실제 예: "(FK)겉절이양념/5kg/냉장/쉐프메이드/씨" + "피케이(H)(5kg*2)"
           "손칼국수/수라식품/1kg(5,7번)(H)(1kg*" + "12)"
  판정 신호 (하나라도 해당하면 이어짐):
   ① 품목명만 있고 수량·단가·공급가액·부가세가 전부 비어 있다
   ② 직전 행의 품목명이 "*", "/", "(", "-" 로 끝난다
   ③ 직전 행의 여는 괄호 개수 > 닫는 괄호 개수
   ④ 그 행의 텍스트가 ")"로 끝나는데 여는 괄호가 없다 (예: "12)")
  처리: 직전 행의 shortName/originalName 뒤에 이어붙이고, 그 행 자체는 items에서 제거.
        병합한 항목은 mergedFrom="H6", uncertain=true 로 표시.
  금지: 위 신호가 없는데 숫자 칸에 값이 있으면 절대 병합하지 마세요 (별개 품목입니다).
  예외: 텍스트가 "합계/소계/부가세/총계/이월/잔액/미수금"이면 H6이 아니라 H4로 처리.
  참고: 표 HTML의 한 셀 안에 <br> 또는 줄바꿈이 있으면 그것은 처음부터 하나의 품목명입니다.
        절대 두 품목으로 쪼개지 마세요.
  ⚠ 병합 후 반드시 STEP 2.5에서 판별한 관계식으로 재검증하세요.

**[H2] 수량·단가 범위 검증**
  수량 > 단가이면 열을 잘못 매핑했을 가능성 → note에 경고
  정상 범위: 수량 0.5~50, 단가 500~200,000

**[H3] 수량 ".000" 오독 보정**
  OCR이 "3.000"을 "3000"으로 읽는 패턴:
  qty가 1000의 배수 AND (qty/1000) × unitPrice ≈ lineTotal → qty를 1000으로 나눔

**[H4] 합계/소계 행 제외**
  품목명이 "합계", "소계", "부가세", "총계"이면 items에 넣지 말고 summary에 반영

**[H5] 체크표시/수기 메모 제거**
  품목명에서 "✓", "○", "*", 불필요 공백 제거 (원본은 originalName에 보존)

### STEP 3.5: 과세/면세 판정

근거가 없으면 반드시 "unknown"으로 두세요. **추측 금지.**
1. **부가세 칸이 1차 신호다.**
   · 값이 있고 > 0 → "taxable"
   · 비어 있거나 0인데 같은 표의 다른 행엔 값이 있음 → "exempt"
2. 품목명 안의 "(면세)" 표기는 보조 확인용.
   ⚠ 면세 품목이라도 품목명에 표기가 없는 경우가 많습니다.
      실제 예: "백합/냉동/1kg(1kg*5개/박스)" — 표기 없지만 부가세 칸이 비어 면세.
      **품목명에 (면세)가 없다는 이유로 과세로 단정하지 마세요.**
3. 세구분 전용 컬럼("과"/"면", "T"/"E", "V"/"X", "10%"/"0%")이 있으면 최우선.
4. 표 전체에 부가세 칸 자체가 없으면 → 전 행 "unknown" (안분 금지)
   (문서 하단에 부가세 총액만 있는 경우도 행 단위로는 unknown입니다.)

면세 품목 참고(판정 보조일 뿐, 위 1~3을 뒤집지 마세요):
쌀·잡곡, 신선 채소·과일, 정육(미가공), 생선·수산물(미가공), 계란, 우유(흰우유), 김치 등
가공식품·조미료·소스·음료·주류·일회용품·소모품은 통상 과세

### 합계행 읽기 (매우 중요)

전표 하단 합계 영역에는 보통 **두 종류의 숫자**가 섞여 있습니다.
  · 당기 거래 축 — 합계금액, 공급가액계, 세액계, 당일거래, 소계
      → summary.grandTotal / totalSupply / totalTax 는 **여기서만** 가져오세요
  · 채권 잔액 축 — 전월이월, 직전미수금, 총미수금, 당월잔액, 잔액, 수금액, 누계
      → 🚫 이번 전표 금액이 **아닙니다**. summary에 넣지 마세요
⚠ 채권 축 값이 훨씬 크고, 거기에도 "합계"라는 글자가 붙는 경우가 많습니다.
   예: "① 직전미수금 | ①+② 합계 | ② 결제합계 | 공급가액합 | 부가세합" 구조에서
       "①+② 합계"는 누적 미수금이지 이번 전표 금액이 아닙니다.
   **가장 큰 숫자 / "합계" 라벨을 근거로 고르지 마세요.**
⚠ 두 축이 **같은 행에 섞여 있는 경우가 흔합니다.** 행 단위가 아니라 **값 단위로** 구분하세요.
   위 예에서 같은 행이라도 "② 결제합계 / 공급가액합 / 부가세합" 세 값은 당기 거래 축이므로
   반드시 summary에 넣어야 합니다. 미수금 라벨이 같은 행에 있다고 행 전체를 버리면 안 됩니다.
✅ 당기 거래 축 값이 문서에 있으면 **반드시 summary에 채우세요** (생략 금지).
   채권 축만 제외하는 것이지, summary 자체를 비우라는 뜻이 아닙니다.
검산: totalSupply + totalTax = grandTotal = Σ(각 행의 결제금액)
      맞지 않으면 채권 축을 잘못 집었는지 먼저 의심하세요.

### STEP 4: 낙서/메모 필터링

Upstage 응답의 "비표 텍스트 블록"에 있는 항목은 원칙적으로 **제외**.
식자재로 보이는 단어(들깨, 사골, 만두 등)가 있어도 표 밖에 있으면 **주문 메모**일 뿐이지 이번 전표의 품목이 아닙니다.

### STEP 5: 한글 품목명 정규화 (Upstage 결과 신뢰)

Upstage 한글 인식률은 높으므로 원본을 **임의로 수정하지 마세요**.
다만 다음만 수행:
- shortName: 규격 분리 (예: "양파10kg" → shortName="양파", spec="10kg")
- 불필요 공백/줄바꿈 제거

## 출력 형식 (순수 JSON, 코드블록 없이)

{
  "counterpartyName": "공급자 상호명 또는 null",
  "transactionDate": "YYYY-MM-DD 또는 null",
  "counterpartyInfo": {
    "contactName": "담당자명 또는 null",
    "contactPhone": "연락처 또는 null"
  },
  "documentType": "거래명세표|거래명세서|영수증|간이영수증|수기전표|배달정산서|기타",
  "items": [
    {
      "shortName": "품목명 (규격 제외)",
      "spec": "규격 (없으면 빈 문자열)",
      "originalName": "Upstage 원본 텍스트 그대로",
      "quantity": "수량(숫자 문자열)",
      "unit": "단위",
      "unitPrice": "단가(숫자 문자열)",
      "lineTotal": "결제 합계금액(공급가+부가세, 숫자 문자열). 합계금액 컬럼이 별도면 그 값 사용. 부가세 컬럼만 있으면 공급가+부가세 합산. 부가세 표기 없으면 공급가액 그대로.",
      "supplyAmount": "공급가액(부가세 제외, 숫자 문자열). 해당 컬럼이 없으면 null",
      "vatAmount": "부가세액(숫자 문자열). 부가세 컬럼이 없거나 판독 불가면 0이 아니라 null",
      "taxType": "taxable | exempt | unknown",
      "uncertain": false,
      "mergedFrom": "H1 등 적용된 보정 규칙 (있을 때만)"
    }
  ],
  "summary": {
    "totalSupply": "합계 또는 null",
    "totalTax": "부가세 합계 또는 null",
    "grandTotal": "총합계 또는 null"
  },
  "note": "비고, 또는 적용된 보정/경고 메모"
}${profileHint}${correctionNote ? `\n\n${correctionNote}` : ""}`;
}

// ────────────────────────────────────────────────────────────────────────────
// v1: Claude Vision 이미지 직접 분석 (fallback)
// ────────────────────────────────────────────────────────────────────────────

export function promptV1ImageFallback(profileHint: string): string {
  return `이 이미지는 한국 식당/매장의 매입 전표입니다.

## 작업 순서 (반드시 이 순서대로 수행하세요)

### STEP 1: 문서 구조 파악
먼저 이미지 전체를 살펴보고 다음을 확인하세요:
- 문서 유형 (거래명세표/거래명세서/영수증/수기전표/배달정산서/기타)
- 표의 열 헤더 위치와 순서 (품목, 규격, 단위, 수량, 단가, 공급가액 등)
- 공급자(판매자) 정보 위치

### STEP 2: 거래처명/날짜 추출
문서 상단에서:
- 거래처명: **공급자(판매자)** 측 상호명 ("공급받는자"가 아님)
- 거래일: YYYY-MM-DD 형식

### STEP 3: 품목별 데이터 추출
표의 **각 행**을 위에서 아래로 하나씩 읽으세요:
- 열 위치를 혼동하지 마세요. STEP 1에서 확인한 열 순서를 따르세요.
- **숫자는 자릿수까지 정확히 읽으세요.** 1과 7, 3과 8, 5와 6, 0과 8을 구분하세요.
- 콤마가 포함된 숫자: "12,000"은 12000이지 12나 1200이 아닙니다.

### STEP 4: 자기 검증
추출 완료 후 각 행에 대해 검증하세요:
- **단가 기준을 먼저 판별하세요** (부가세 값이 있는 과세 행으로):
  ⓐ 수량×단가 = 공급가액+부가세  → 단가가 부가세 포함 (단가 > 공급가액)
  ⓑ 수량×단가 = 공급가액         → 단가가 부가세 별도 (단가 ≤ 공급가액)
  판별한 유형을 문서 전체에 일관 적용. 행마다 다르게 해석하지 말 것.
  면세 행은 두 유형 모두 공급가액 = 수량×단가, 부가세 공란입니다.
- lineTotal = 결제금액 = 공급가액 + 부가세 (유형 무관)
- 수량은 보통 0.5~50, 단가는 보통 500~200,000 범위
- 수량이 단가보다 크면 열을 잘못 읽은 것 → 재확인
- 합계행의 금액과 개별 lineTotal의 합이 비슷한지 확인

## ⚠ 한글 인식 규칙

**품목명 줄바꿈**: 한 품목명이 칸 안에서 두 줄로 접혀 있으면 하나의 품목입니다.
아랫줄에 수량·단가·금액이 없으면 윗줄 품목명의 연속입니다. 별개 항목으로 세지 마세요.

**글자 단위로 정확히 읽으세요:**
- 받침: "달" ≠ "닭", "곤" ≠ "콩", "갈" ≠ "감", "봉" ≠ "볶", "란" ≠ "단"
- 모음: "대" ≠ "데", "래" ≠ "레", "파" ≠ "피", "무" ≠ "두", "배" ≠ "베"
- 초성: "깨" ≠ "째", "감" ≠ "강", "돈" ≠ "론", "불" ≠ "볼"
- 겹받침: "닭" ≠ "달", "삶" ≠ "살", "읽" ≠ "일"

**식자재 참고 단어집:**
채소: 양파, 감자, 대파, 쪽파, 당근, 양배추, 깻잎, 마늘, 생강, 고추, 청양고추, 오이, 배추, 시금치, 무, 부추, 미나리, 콩나물, 숙주, 브로콜리, 파프리카, 토마토, 호박, 애호박
육류: 삼겹살, 목살, 안심, 등심, 갈비, 닭가슴살, 닭다리, 닭날개, 오리, 소고기, 돼지고기, 항정살, 차돌박이
수산: 새우, 오징어, 고등어, 연어, 참치, 조개, 굴, 문어, 멸치, 꽃게, 전복, 갈치, 광어
가공: 두부, 우유, 계란, 치즈, 버터, 참기름, 들기름, 식용유, 올리브유, 마요네즈, 케첩
조미: 소금, 설탕, 간장, 된장, 고추장, 쌈장, 식초, 후추, 물엿, 맛술, 굴소스, 칠리소스
곡물: 밀가루, 쌀, 면, 국수, 라면, 떡, 만두, 부침가루, 전분
음료: 콜라, 사이다, 맥주, 소주, 생수, 주스, 탄산수, 에이드
소모품: 일회용, 랩, 호일, 봉투, 장갑, 행주, 세제, 소독, 키친타올, 물티슈

**숫자와 한글 분리:**
- "양파10kg" → 품목: "양파", 규격: "10kg"
- "대파1단" → 품목: "대파", 수량: "1", 단위: "단"

**불확실한 글자:** 가장 가능성 높은 식자재명으로 추정 + uncertain: true

## 문서 양식

**[A] 거래명세표** — 가장 흔함. 열 구성은 업체마다 다르므로 **헤더를 먼저 읽을 것**.
  흔한 예1: 품목/규격 | 단위 | 수량 | 단가 | 공급가액 | 부가세 | 비고  (월일·합계금액 컬럼 없음)
  흔한 예2: 월일 | 품목 | 규격 | 수량 | 단가 | 공급가액 | 세액 | 합계금액
  합계금액 컬럼이 따로 있으면 그 값을 lineTotal로 우선 사용.
  없으면 공급가액 + 부가세로 합성.
**[B] 거래명세서** — 열: 품명|규격|수량|단가|공급가액. 수기 혼합 가능.
**[C] 영수증** — 품명, 수량, 금액. 합계행 제외.
**[D] 수기전표** — 손글씨. 열 구분 불명확 시 uncertain: true.
**[기타]** — 열 헤더 먼저 읽고, 구조를 note에 기록.

## 합계행 읽기 (매우 중요)

전표 하단 합계 영역에는 보통 **두 종류의 숫자**가 섞여 있습니다.
  · 당기 거래 축 — 합계금액, 공급가액계, 세액계, 당일거래, 소계
      → summary.grandTotal / totalSupply / totalTax 는 **여기서만** 가져오세요
  · 채권 잔액 축 — 전월이월, 직전미수금, 총미수금, 당월잔액, 잔액, 수금액, 누계
      → 🚫 이번 전표 금액이 **아닙니다**. summary에 넣지 마세요
⚠ 채권 축 값이 훨씬 크고, 거기에도 "합계"라는 글자가 붙는 경우가 많습니다.
   예: "① 직전미수금 | ①+② 합계 | ② 결제합계 | 공급가액합 | 부가세합" 구조에서
       "①+② 합계"는 누적 미수금이지 이번 전표 금액이 아닙니다.
   **가장 큰 숫자 / "합계" 라벨을 근거로 고르지 마세요.**
⚠ 두 축이 **같은 행에 섞여 있는 경우가 흔합니다.** 행 단위가 아니라 **값 단위로** 구분하세요.
   위 예에서 같은 행이라도 "② 결제합계 / 공급가액합 / 부가세합" 세 값은 당기 거래 축이므로
   반드시 summary에 넣어야 합니다. 미수금 라벨이 같은 행에 있다고 행 전체를 버리면 안 됩니다.
✅ 당기 거래 축 값이 문서에 있으면 **반드시 summary에 채우세요** (생략 금지).
   채권 축만 제외하는 것이지, summary 자체를 비우라는 뜻이 아닙니다.
검산: totalSupply + totalTax = grandTotal = Σ(각 행의 결제금액)
      맞지 않으면 채권 축을 잘못 집었는지 먼저 의심하세요.

## 추출 필드

각 품목:
- shortName: 품목명만 (규격 제외). 예: "핫철리소스/CHOLIMEX/250g" → "핫철리소스"
- spec: 규격 (용량/중량/사이즈). 없으면 ""
- originalName: 이미지 원본 텍스트 그대로
- quantity: 수량 (숫자, 콤마 제거, 소수점 유지. "6.000"→"6")
- unit: 단위 (규격에서 유추 가능하면 반영)
- unitPrice: 단가 (숫자, 콤마 제거)
- lineTotal: 합계금액 = 공급가 + 부가세 (숫자, 콤마 제거). 합계금액 컬럼이 따로 있으면 그것을 우선. 부가세가 별도 컬럼이면 합산. 부가세 표기 없으면 공급가액 그대로.
- supplyAmount: 공급가액(부가세 제외). 해당 컬럼이 없으면 null
- vatAmount: 부가세액. 부가세 컬럼이 없거나 판독 불가면 0이 아니라 null
- taxType: "taxable"(과세) | "exempt"(면세) | "unknown". 전표에 세구분/행별 부가세 근거가 없으면 반드시 "unknown". 추측 금지.
- uncertain: 불명확하면 true

거래처/기타:
- counterpartyName: 공급자 상호명
- transactionDate: YYYY-MM-DD (없으면 null)
- contactName/contactPhone: 담당자 정보 (없으면 null)
- summary: totalSupply, totalTax, grandTotal
- 합계/소계 행은 items에 넣지 말고 summary에

## 출력 형식 (순수 JSON만, 코드블록 없이):
{
  "counterpartyName": "거래처명 또는 null",
  "transactionDate": "거래일 YYYY-MM-DD 또는 null",
  "counterpartyInfo": {
    "contactName": "담당자명 또는 null",
    "contactPhone": "연락처 또는 null"
  },
  "documentType": "거래명세표|거래명세서|영수증|간이영수증|수기전표|배달정산서|기타",
  "items": [
    {
      "shortName": "품목명/규격 (규격 없으면 품목명만)",
      "spec": "규격 (용량/중량/사이즈/포장단위, 없으면 빈 문자열)",
      "originalName": "이미지 원본 텍스트",
      "quantity": "수량(숫자)",
      "unit": "단위 (규격에서 유추 가능하면 반영)",
      "unitPrice": "단가(숫자)",
      "lineTotal": "합계금액 = 공급가 + 부가세 (숫자, 콤마 제거). 합계금액 컬럼이 따로 있으면 그것을 우선. 부가세가 별도 컬럼이면 합산. 부가세 표기 없으면 공급가액 그대로.",
      "supplyAmount": "공급가액(부가세 제외, 숫자) 또는 null",
      "vatAmount": "부가세액(숫자) 또는 null",
      "taxType": "taxable | exempt | unknown",
      "uncertain": false
    }
  ],
  "summary": {
    "totalSupply": "공급가액 합계(숫자) 또는 null",
    "totalTax": "부가세 합계(숫자) 또는 null",
    "grandTotal": "총합계(숫자) 또는 null"
  },
  "note": "비고 또는 null"
}${profileHint}`;
}
